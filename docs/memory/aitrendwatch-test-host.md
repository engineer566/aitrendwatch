---
name: aitrendwatch-test-host
description: aitrendwatch 测试主机（47.98.124.167）SSH/部署细节
metadata:
  node_type: memory
  type: reference
  originSessionId: 9940b4e2-c608-4fb4-9867-93b67017b944
---

- **IP**：`47.98.124.167`
- **SSH**：`ssh -i ~/Projects/test_host.pem root@47.98.124.167`（key 需 `chmod 600`，否则 ssh 拒绝）
- **目录**：`/opt/aitrendwatch`
- **编排**：`docker-compose.test.yml`（测试专用，直接暴露 `0.0.0.0:8080:5000`，2 worker，不依赖 Nginx）
- **访问**：`http://47.98.124.167:8080`
- **容器名**：`aitrendwatch`，基础镜像 `python:3.13-slim`
- **.env**：已生成随机 `SECRET_KEY` 和 `ADMIN_TOKEN`（ADMIN_TOKEN 见部署时回显，或服务器 `/opt/aitrendwatch/.env`）
- **主机环境**：Ubuntu 22.04 / x86_64 / 1.6G 内存 / Docker 29.7.2 + Compose v5.5.0

部署：`ssh -i ~/Projects/test_host.pem root@47.98.124.167`（本机实际 key 路径为 `/data/test_host.pem`，不是 `~/Projects/`）→ `cd /opt/aitrendwatch` → `docker compose -f docker-compose.test.yml up -d` → `docker compose -f docker-compose.test.yml logs -f`。测试主机与生产主机是**两台独立机器**，不要混淆 SSH key。

**重要（2026-08-28 验证）**：`/opt/aitrendwatch` **不是 git clone**，是文件拷贝部署；compose 用 bind mount 逐个挂载 `.py` 文件。**每次合入新增模块（如 `terms.py`）时，必须在 `docker-compose.test.yml` 里补挂载**（`- ./terms.py:/app/terms.py:ro`），否则容器内 `import` 失败、功能静默降级。本地仓库没有 `docker-compose.test.yml`（只在主机上手工维护）。

**公网 8080 已放行（2026-08-28 阿里云安全组开通后验证）**：SSH(22) 与 `http://47.98.124.167:8080` 均可公网访问。此前 8080 公网不可达的根因是**阿里云安全组未放行 8080**（主机 iptables/UFW 无拦截、docker-proxy 监听 `0.0.0.0:8080`）——主机本机 curl 自己公网 IP 也超时的现象即安全组拦截。若再遇到公网访问超时，先查阿里云控制台安全组，而非主机防火墙。

**2026-08-29（dev 合入 glm-switch + memory-opt 后部署）**：`/opt/aitrendwatch/.env` 已配置 `GLM_API_KEY`（智谱 BigModel，值不记录于此）。应用默认走 `LLM_CHAIN`（`glm-4.7-flash` 首档），**未设 DeepSeek key**。

**2026-08-30（修复中英文混杂）**：测试机更换为**独立 GLM key**（不再与生产共用，避免两机同时刷新互挤免费档配额），`.env` 另设 `DIMS_REFRESH_HOURS=2,8,14,20`（与生产 1,7,13,19 错开）。仍**未设 DeepSeek key**——测试机混杂率上限受 GLM 免费档质量约束（实测独立 key 后 zh 语境混杂 64%→32%，剩余为 GLM 偶发 429/超时且无备用 provider）。

**智谱 GLM 免费档限流现象（实测）**：`glm-4.7-flash` 高峰返 `1305 访问量过大`、账户级返 `1302 速率限制`（同 key 所有 GLM 档都受限）；1-token 直测可成功但批量分类请求被限。属**临时性**——限流时应用按设计走故障转移链 → 优雅降级（`title_zh=原标题`、`dimension=default_dim`），服务不中断、缓存照写。下次测试 GLM 若再遇 1302/1305，等限流窗口重置或改非高峰时段即可，不是 key/代码问题。

**2026-09-02（history/20260902 四需求部署验证）**：Windows 本机 `git -c core.autocrlf=false archive dev` 导出 LF 干净树 → scp 变更文件（app.py/terms.py/news_store.py/templates 2 个 html）→ `docker compose -f docker-compose.test.yml up -d --force-recreate`。验证结果：① 回归清单记忆（仓库文档）；② Openclaw 收录词池（最热榜 #72、上升榜 #93 均在 100 词窗口，`/term/openclaw` 有词典解释）；③ 英文页点击链路词名全英文（display_en 保留修复）；④ `/term/gemini`、`/term/huggingface` 不再显示 `-1.00`（真实下跌仍显示）。**实测注意**：容器重启后启动预热刷新约 15-25 分钟（17+ 源 RSS → HN/Reddit enrich → GLM 批量打标 ~40s/批 → 词聚合 → 解释批次），期间 `cache/` 三个 json 逐个更新（terms.json → dims.json → words.json），以 words.json 更新为刷新完成标志；GLM-4.7 慢/限流时自动切 glm-5.3-flash（批速 ~15s）。`.env` 现为 `DIMS_REFRESH_HOURS=1,7,13,19`（与生产相同；此前记录 2,8,14,20 已改）。

**2026-09-05（history/20260904 四需求部署验证）**：dev（`ae9ba02`）合入 task1~task4 后部署。本机 `git -c core.autocrlf=false archive dev` 导出 LF 树 → scp 运行时文件（app/config/dims/tracker/terms/store/news_store/text_utils/stream_utils/version/VERSION + templates 4 个 html）→ `docker compose -f docker-compose.test.yml up -d --force-recreate`。**注意（踩坑）**：首启 worker 崩溃——`ImportError: cannot import name 'LLM_QUALITY_CYCLE_ESCAPE' from 'config'`：测试机 `config.py` 是 09-02 旧版（09-03 质量/可用性分离常量在仓库 config 里但此前未随部分部署同步），单传 dims.py 等会引入对旧 config 的依赖 → 部署必须把 config.py 等全部运行时文件一起同步，不要只传改动文件。验证结果：① 需求 1（同词条重复报道）——重启 init_db 自愈后 news.db 实体孪生行 0（此前 &amp;/&、utm 变体双插 9 组标题重复已消除，剩 3 组为跨源镜像：qbitai/infoq 千问办公、sspai 会员页、geekpark 369800/369867）；刷新后词卡 top-100 `/api/word` 标题级去重 0 重复、`/api/stream?view=news` 434 卡 0 重复、搜索（`q=hugging face` 46 命中）news 标题 0 重复；② 需求 2（榜单重复词条）——根因=分隔符孪生 canonical（hugging-face/huggingface），刷新后词池 200 词 0 同展示名、terms 表 `hugging-face` 残留行删除仅剩 `huggingface` 1 行、`/api/word/hugging-face` 与 `/huggingface` 同解析（Hugging Face，21 篇）；③ 需求 3（按钮英文）——`/`、`/term/agent`、`/search`、`/hf` × zh/en 8 组合按钮 aria-label 与可见文案均为 "Back to top"，无 回到顶部；④ 需求 4（中文公司名英译）——容器内 `_COMPANY_EN_GLOSSARY` 37 键实测（创通联达→Thundercomm、中科创达→ThunderSoft、深度求索→DeepSeek、智谱AI→Zhipu AI、月之暗面→Moonshot AI），未收录中文专名（如 曲靖科技）返回空不拼音化，5.6 词典预写不进 LLM 批次由单测覆盖；QA 两案例（Thundercomm/Qujing Tech）已轮换出窗口，词条-公司对应关系待真实数据复现后再确认。**回归**：dev 全量 217 tests + 8 subtests 全绿（无 key）。**注意**：本次重启预热 tracker ~2.5min（terms.json 04:20）、dims ~19min（words.json 04:37 完成标志），LLM 全部走 glm-5.3-flash（含 1 次 JSON 解析失败经二次修正通过）；terms.py 现 2234 行、dims.py 1852 行。

**2026-09-03（需求 5~6 改进部署验证）**：dev 合入 task5b（词典外词 display 保留原文大小写）+ task6b（回到顶部按钮文字化 + 移动端优化）后部署。scp terms.py + templates 4 个 html → `--force-recreate`。验证结果：⑥ 改进——4 页面（`/`、`/term/openclaw`、`/search`、`/hf`）按钮为文字胶囊「↑ 回到顶部 / ↑ Back to top」（aria-label 双语），含 rAF 滚动节流 + matchMedia 窄屏阈值 250/宽屏 400 + `env(safe-area-inset-*)` 安全区，四页块逐字节一致；⑤ 改进——容器内确定性验证全过（workbuddy 新词/存量升级→"WorkBuddy"、irobot→"iRobot"、openai 全小写标题不被污染仍 "OpenAI"、全大写标题党 "IROBOT" 不入选）；真实数据 terms 表复验：spacex→SpaceX、javascript→JavaScript、mujoco→MuJoCo、amazon-bedrock-agentcore→Amazon Bedrock AgentCore、harmonyos-7→HarmonyOS 7 等词典外词全部保留原文大小写；**验证中发现并修复**：词典权威词存量脏 display（早期 pretty 把 SaaS/DevOps 顶成 Saas/Devops）→ dev 直接 fix（display 以词典规则为准）+ 新增测试后重部署，复验 SaaS→SaaS、DevOps→DevOps 修正。**回归**：dev 全量 180 tests + 8 subtests 全绿（无 key）。**注意**：WorkBuddy 词在测试机未进池（LLM 抽词覆盖问题，news_cards 有 4 条 WorkBuddy 报道但 keywords 未含该词，属抽词行为非 display 逻辑），display 修复已由容器确定性用例覆盖；terms.py 现 1840 行。

**2026-09-02（history/20260902 需求 5~6 部署验证）**：dev 合入 task5（关键词大小写硬编码校验）+ task6（悬浮回到顶部按钮）后部署。本机 `git -c core.autocrlf=false archive dev` 导出 LF 树 → scp terms.py/dims.py/templates 4 个 html → `docker compose -f docker-compose.test.yml up -d --force-recreate`（镜像仍 `aitrendwatch:runtime`，无依赖变更无需 build）。验证结果：⑤ 需求 5——容器内确定性校验全过（`extract_keywords_dict`：`OpenAI releases GPT-5` → `["GPT-5","OpenAI"]`、`OpenClaw Releases OpenClaw 2.0: Guided Model Setup` → `["OpenClaw"]`、`GLM-5 model …` → 含 `GLM`；`case_match_original` 命中原文取原文确切大小写 / 未命中保持 canonical / CJK 原样 / 空格变体 `GPT 5`；`_news_row_canons` 聚合键仍 canonical `{"gpt-5","openai"}` 不分裂）；预热完成后 `/api/stream?view=news` 当轮卡 keywords 与标题大小写一致性脚本校验 **64 卡 / 53 关键词 / 0 不一致**；词池无大小写重复词、无停用词。⑥ 需求 6——`/`、`/term/openclaw`、`/search?q=`、`/hf` 四页公网 200，`.back-top` 按钮 HTML/CSS/JS 均在，aria-label/title 中英双语（Back to top / 回到顶部）正确；滚动浮现/平滑回顶交互待人工浏览器确认。**回归**：dev 全量 164 tests + 8 subtests 全绿（无 key）。**注意**：本次重启预热 tracker ~2min（terms.json 22:21:53）、dims ~9min（words.json 22:28:26 完成标志）；GLM-4.7 高峰 429×3 切 glm-5.3-flash（多批成功），部分批次中英混杂按失败计 → 周期累计 4 次逃到 deepseek（测试机无 key）→「末档无 key 回绕首档」循环 + 翻译/解释降级——GLM 免费档限流的既有临时现象，非代码问题；关键词大小写在 LLM 路径（dims.py 收口）与降级词典路径（extract_keywords_dict 收口）下均验证一致。

**2026-09-01（release 1.8.0 部署验证，20260901 需求 8~11）**：本机 `git -c core.autocrlf=false archive main` 导出 LF 干净树 → `rsync --checksum` 同步运行时文件（app.py/terms.py/store.py/VERSION/templates 6 个 html + requirements.txt/Dockerfile）→ **镜像改为 `aitrendwatch:1.8.0`**（本机 `docker build`，requirements 含 pillow）→ `docker-compose.test.yml` 的 `image:` 由 `python:3.13-slim` 改为 `aitrendwatch:1.8.0` → `docker compose -f docker-compose.test.yml up -d --force-recreate`。**逐项验证结果**：① 任务 8（大小写统一）——容器内 `normalize_term`：gpu/Gpu→GPU、ui/Ui→UI、glM→GLM、sdk→SDK、openai→OpenAI、huggingface→Hugging Face、moe→MoE、aqua→AQuA、npu→NPU 全对；`/api/stream?view=words` 词池无小写缩写残留，GPU/GLM 在榜；`/term/gpu`、`/term/glm` 200；**存量清理**——terms 表旧 `('gpu','Gpu')/('glm','Glm')/('MOE','MOE')` 小写/脏 display 已用 SQL 合并为规范键 + 强制 display 对齐（GPU/GLM/MoE）；② 任务 9（埋点）——`/api/event` 单条/批量均 ok:true，白名单外 event_type 落库被拒（`_VALID_EVENT_TYPES`），`sponsors.db.user_events` 落库验证（page_view/click/word_expand/search），`/monitor/api/events` 返回 by_type/daily/recent；③ 任务 10（monitor+admin 合并）——`/admin` 302→`/monitor#sponsors`，`/monitor` 渲染双 Tab，赞助位 create/list/toggle/delete 全通（表单格式：`slot_id` 必填），`/monitor/api?days=30` 正常；④ 任务 11（SEO）——首页/term/hf 页 title/description/OG/Twitter/canonical 齐备，ld+json 全合法（WebSite+ItemList / DefinedTerm+ItemList / CollectionPage+ItemList），robots.txt 含 Sitemap 行，sitemap.xml 正常，404 页 noindex；**og-image 修复**——原容器无 pillow 降级返回 favicon(223B)，requirements 补 `pillow==12.3.0` + 构建 1.8.0 镜像后返回 1200×630 PNG（7849B）。**回归**：155 tests + 8 subtests 全绿（无 key 降级），三榜 rise/hot/new 排序正确，详情页解释块/报道 hot 降序/news_cnt 一致，搜索高亮、display_en 保留、Rise -1.00 隐藏正常。**注意**：容器已改走 `aitrendwatch:1.8.0` 本地镜像（非基础镜像），后续 `docker compose up -d` 不会因 recreate 丢 pillow；`config/terms_canonical.json` 是构建期事实源（sync 脚本写入 terms.py），运行时不挂载。

相关：[`aitrendwatch-deploy-key`](aitrendwatch-deploy-key.md)、[`hot-aggregator-aitrendwatch`](hot-aggregator-aitrendwatch.md)、[`aitrendwatch-server-stability`](aitrendwatch-server-stability.md)、[`aitrendwatch-regression-checklist`](aitrendwatch-regression-checklist.md)
